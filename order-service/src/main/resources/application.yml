
server.port: 8000

spring:
  application.name: order-service
  data.mongodb: #.uri: mongodb://order-user:order-password@localhost:27001,localhost:27002,localhost:27003/order-db
      host: localhost:27017
      database: order_db
      username: order_user
      password: 123qwe

kafka:
  receiver:
    bootstrap-servers: localhost:9092
    group-id: checked-orders-order-service-group
  sender:
    bootstrap-servers: ${kafka.receiver.bootstrap-servers}
  topics:
    new-orders-topic:
      name: new-orders-topic
      partitions: 1
      replicas: 1
    checked-addresses-topic:
      name: checked-addresses-topic
      partitions: 1
      replicas: 1
    checked-couriers-topic:
      name: checked-couriers-topic
      partitions: 1
      replicas: 1
    checked-menu-orders-topic:
      name: checked-menu-orders-topic
      partitions: 1
      replicas: 1
    checke-order-success-topic:
      name: checke-order-success-topic
      partitions: 1
      replicas: 1

#eureka:
#  instance.preferIpAddress: true
#  client:
#    registerWithEureka: true
#    fetchRegistry: true
#    serviceUrl:
#      defaultZone: http://discovery-service:8761/eureka
